module.exports = {
  aws_table_name: 'dynamodb-test',
  aws_local_config: {
    //Provide details for local configuration
  },
  aws_remote_config: {
    accessKeyId: process.end.KEY,
    secretAccessKey: process.end.SKEY,
    region: 'us-west-2',
  },
};
